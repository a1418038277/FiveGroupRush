<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="firsttest.test.fivegroupproject">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".app.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.homeworkreviews.HomeWorkReviewActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.myorder.MyorderActivity">

        </activity>
        <activity android:name=".ui.billingdetails.BillingDetailsActivity"></activity>
        <activity android:name=".ui.myhomework.MyHomeWorkActivity" />
        <activity android:name=".ui.eventdetail.EventDetailActivity" />
        <activity android:name=".ui.id.IDActivity" />
        <activity android:name=".ui.about.AboutActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>